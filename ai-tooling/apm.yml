name: ai-tooling
version: 0.0.2
description: >
  Meta-tooling for APM package authors — retrofit existing skills/plugins/agents
  repos into APM-compliant packages, scaffold manifests, and audit compliance.
